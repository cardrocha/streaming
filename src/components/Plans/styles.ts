import styled from 'styled-components'
import { colors } from '../../../styles'

export const Title = styled.h2`
  font-size: 36px;
  text-align: center;
  color: ${colors.white};
  margin-top: 120px;
`
