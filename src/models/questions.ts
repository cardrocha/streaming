class Question {
  id: number
  title: string
  answer: string

  constructor(id: number, title: string, answer: string) {
    this.id = id
    this.title = title
    this.answer = answer
  }
}

export default Question
